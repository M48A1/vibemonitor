# VibeMonitor v1.0.48 — 全站主题管理与 Hex 主题

本版本完善全站主题管理，提供默认与 Hex 两套内置主题，并支持由管理员统一设置深浅色模式。

- **全站主题管理**：管理员在“管理 → 主题管理”选择主题和明暗模式，保存后同步到访客页面。
- **Hex 主题**：提供节点状态、周期流量、实时网速与高负载总览，支持搜索、状态筛选及手机布局。
- **主题持久化**：设置保存到 SQLite，重启与备份恢复后保留；旧数据库自动使用默认主题和深色模式。
- **主题健壮性修复**：校验页面主题占位符，统一前端主题白名单，增强设置表单容错并完善设置变更广播。
- **测试与许可声明**：补充主题存储、页面渲染及前端行为检查，附带 Hex 参考项目许可声明。

## 更新方式

先下载本次 Release 的新版 `install.sh`，再执行：
```bash
sudo bash install.sh update -p 1314
```
（端口请按实际部署修改）。更新前建议先执行 `sudo bash install.sh backup` 备份数据库。
