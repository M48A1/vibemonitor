# VibeMonitor v1.0.50 — 精简 Hex 监控界面

- **简化统计区标题**：移除“服务器总览”，保留“实时监控”及统计卡片。
- **新增节点明细标题**：以与“实时监控”一致的绿点、绿色文字样式显示“节点明细”。
- **移除状态筛选与计数**：删除“全部状态”下拉框和节点计数，节点列表直接展示全部节点。
- **简化顶部导航**：移除“管理”按钮旁的上下行实时网速显示。

## 更新方式

先下载本次 Release 的新版 `install.sh`，再执行：
```bash
sudo bash install.sh update -p 1314
```
（端口请按实际部署修改）。更新前建议先执行 `sudo bash install.sh backup` 备份数据库。
