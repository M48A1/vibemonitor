# VibeMonitor v1.0.44 — 性能与资源占用深度优化

本版本针对磁盘 I/O、网络传输、长连接广播、历史图表渲染以及探针内存占用进行了全链路优化：

- **SQLite 刷盘优化**：WAL 模式下将写入参数调整为 `synchronous=NORMAL`，消除每秒 Ping 采样频繁触发的物理 `fsync` 刷盘等待，写性能提升数十倍并极大保护 SSD。
- **服务端 Gzip 传输压缩**：内置基于 `sync.Pool` 的高效零锁 Gzip 中间件，静态前端文件（JS/CSS/HTML）与 JSON API 传输体积节省 75% 以上，显著加快首屏加载速度。
- **WebSocket 广播空转与协程优化**：增加数据变动检测，节点指标未变动时跳过全量 JSON 发送；每个客户端采用独立带缓冲通道平滑发送，消除并发短命协程。
- **时序图服务端分桶降采样**：针对 7 天与 31 天大跨度时序数据实施峰值与丢包保护的分桶降采样，单次历史请求从 3MB 降至几十 KB，消除前端绘制卡顿，且卡片统计指标依然保持 100% 全量精确计算。
- **探针 Agent 内存分配复用**：网络连接统计引入 `sync.Pool` 复用扫描缓冲区，进程统计改为批量流式读取，消除高频堆内存申请，将探针常驻内存控制在更低水平。

## 更新方式

先下载本次 Release 的新版 `install.sh`，再执行：
```bash
sudo bash install.sh update -p 1314
```
（端口请按实际部署修改）。更新前建议先执行 `sudo bash install.sh backup` 备份数据库。
