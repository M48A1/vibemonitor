# VibeMonitor v1.0.42 — 可靠性与流量统计修复

本版本适用于已完成 SQLite 迁移的部署，包含本轮确认的五项修复。

- 设置统一校验并在事务中保存，避免图标保存失败导致密码部分更新；改密成功后撤销所有旧登录。
- 流量统计默认排除常见隧道、容器、网桥、VLAN 子接口及 bond 从接口，支持 `--interfaces eth0` 或 `VIBEMONITOR_INTERFACES` 明确指定统计网卡。统计范围改变时只重建基线，保留历史累计用量，不回算旧统计误差。
- 探针基础信息不再只重试五次：恢复连接后补报，并每 15 分钟刷新；退出时取消正在进行的上报请求。
- 上传的站点图标存入 SQLite，与配置一起原子保存并随数据库备份恢复。升级启动时导入当前引用的旧本地图标，保留原文件；后续不再依赖独立图标文件。
- 图标上传支持完整 2 MiB 文件，额外预留 multipart 开销，其他 API 仍保持 1 MiB 请求上限；上传或删除失败不破坏原图标。

旧版 SQLite 备份仍可恢复，但没有内嵌图标内容，换机器恢复后可能需要重新上传图标。外部图标只备份链接。旧 JSON 数据文件及 JSON 备份仍不支持。

## 更新方式

先下载本次 Release 的新版 `install.sh`，再执行 `sudo bash install.sh update -p 1314`（端口按实际部署修改）。请先更新主控，再更新或重启探针，以支持新的流量计数基线标记；同机探针共用程序文件，重启后使用新版。复杂网络建议明确指定出口网卡，不要同时统计物理网卡和同一流量经过的隧道。

更新前请保留可用备份，更新后执行 `sudo bash install.sh backup` 生成包含图标的新 SQLite 备份。`server` 命令为清空重装，请勿用于保留数据更新。未完成 SQLite 迁移的旧部署仍需先使用 v1.0.40。
