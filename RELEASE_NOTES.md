# VibeMonitor v1.0.49 — 统一使用浅色 Hex

- **仅保留 Hex**：移除默认主题，所有页面固定使用浅色 Hex。
- **简化管理设置**：移除主题选择及全局明暗模式设置，保留网站名称、Logo 和管理员密码管理。
- **精简节点界面**：移除搜索框、分组筛选、分组标签及分组编辑栏；保留在线 / 离线筛选。
- **兼容旧数据**：旧数据库和备份恢复后统一使用浅色 Hex，编辑节点时保留已有分组数据。

## 更新方式

先下载本次 Release 的新版 `install.sh`，再执行：
```bash
sudo bash install.sh update -p 1314
```
（端口请按实际部署修改）。更新前建议先执行 `sudo bash install.sh backup` 备份数据库。
