# VibeMonitor v1.0.43 — 延迟波动曲线时间选项优化

本版本更新了延迟波动曲线的时间跨度筛选选项：

- 延迟波动曲线时间选项调整为：**最近一小时**、**最近24小时**、**最近7*24小时**、**最近31*24小时**。
- 后端 `/api/nodes/ping-history` 接口扩展支持 31 天（`31d` / `31*24h`）时序查询与离线区间检测。
- 前端图表展示自适应不同时间窗口，支持按时间点悬浮查看延迟及探针离线状态。

## 更新方式

先下载本次 Release 的新版 `install.sh`，再执行：
```bash
sudo bash install.sh update -p 1314
```
（端口请按实际部署修改）。更新前建议先执行 `sudo bash install.sh backup` 备份数据库。
