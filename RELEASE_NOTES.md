# VibeMonitor v1.0.52 — 流量统计与服务生命周期修正

- **修正周期流量**：探针按网卡上报累计计数器和系统重启标识；主控按采样时间归属账期，在跨账期的上报间隔内按时间比例估算流量。网卡计数器重置或接口集合变化时分别处理，避免重复计数或丢失其他网卡的增量。
- **支持人工校准**：在节点管理的编辑窗口填写当前周期已用流量；留空保持原值，填写 `0` 清零。主控记录校准时间，之后继续累加新流量。
- **改进网卡选择**：默认排除常见虚拟和重复计数接口，可通过探针的 `--interfaces` 或 `VIBEMONITOR_INTERFACES` 指定网卡。接口读取失败时不提交零流量报告。
- **收紧探针认证**：RPC 上报仅接受 `Authorization: Bearer` 或 `X-Token` 请求头；带 `?token=` 的 RPC 请求会被拒绝。仍使用查询参数传递密钥的旧探针需要升级。
- **完善退出处理**：主控退出时停止 WebSocket hub，并等待后台保存任务完成；后台保存失败会记录日志。

## 更新方式

建议先执行 `sudo bash install.sh backup` 备份主控数据库。下载本次 Release 的新版 `install.sh`，先更新主控：
```bash
sudo bash install.sh update -p 1314
```
端口请按实际部署修改。随后在各节点使用新版安装脚本和原有节点 Token 更新探针，以启用新的采样时间、重启标识和网卡统计。旧版探针仍可上报合计计数器，但没有新版修正；使用 RPC 查询参数传递 Token 的旧探针将无法上报。
