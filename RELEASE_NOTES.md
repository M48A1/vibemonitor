# VibeMonitor v1.0.40

## 升级与数据清理

旧 JSON 成功导入 SQLite 并通过完整性、配置、节点及有效延迟样本校验后，程序会删除旧主 JSON、配套 `.ping.json`，以及同一数据目录下整个 `backups` 目录（包括无后缀备份和子目录）。删除不保留恢复副本；需要留存的历史备份，请在升级前复制到其他目录或机器。

迁移或校验失败时保留原文件和备份。清理仅在这次旧数据迁移后执行，普通重启不会删除后来创建的备份。已有初始化数据库时以数据库为准，不会再次导入或清理旧 JSON；全新安装直接使用 SQLite。

## 修复与优化

- 修复旧 JSON 未导入、安装器恢复未实际更新数据库的问题。
- 备份从数据库导出配置、节点、流量和全部留存延迟历史；恢复使用事务，启动失败时回退。
- 修复未配置 TCP 目标的节点在重启后丢失账单信息的问题。
- 修复密码哈希可被直接当作登录密码的问题，保留旧明文密码迁移支持。
- 配置、节点和相关历史清理统一事务提交；跳过未变化的节点与历史样本，减少重复写入。
- 数据库及 WAL/SHM 文件使用 0600 权限，增加迁移、备份恢复、故障回滚和清理边界测试。

## 更新方式

下载本次 Release 的 `install.sh`，执行 `sudo bash install.sh update -p 1314`（端口按当前部署修改）。保留数据更新请使用 `update`；`server` 命令是清空重装。

本版本发布 Linux amd64 程序、安装器和 SHA-256 校验清单。发布前由 GitHub Actions 执行 Linux 竞态测试、前端测试和安装器测试。
