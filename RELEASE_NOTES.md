# VibeMonitor v1.0.51 — 站点图标统一管理入口

- **合并导航入口**：将管理、节点管理和退出登录统一收进左上角站点图标的下拉菜单，移除页面上原有的独立按钮。
- **保留节点操作**：节点管理菜单继续提供编辑现有节点和新建节点；未登录时仅显示管理入口。
- **完善菜单交互**：点击菜单操作后自动收起，支持点击空白处、焦点离开或按 Esc 收起菜单。
- **更新空列表提示**：引导用户从站点图标进入节点管理。

## 更新方式

先下载本次 Release 的新版 `install.sh`，再执行：
```bash
sudo bash install.sh update -p 1314
```
（端口请按实际部署修改）。更新前建议先执行 `sudo bash install.sh backup` 备份数据库。
